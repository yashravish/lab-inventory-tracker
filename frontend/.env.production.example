# Frontend production env. Vite bakes VITE_* vars in at build time, so this
# must be set in Railway as a *Build Variable* on the frontend service — not
# a runtime variable. Use the backend's public Railway URL (no trailing slash).
VITE_API_BASE_URL=https://your-backend.up.railway.app
